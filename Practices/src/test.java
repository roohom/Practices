public class test {
    public static void main(String[] args){
        int age;
        age = 19;
        System.out.println("println的打印输出会自动换行，并且可以不接收参数");
        System.out.print("print的打印输出不会自动换行，并且其必须接收一个参数，如果不接收参数，在执行的时候会报错！");
    }
}
